{% extends 'base.html.twig' %}

{% block title %}Editar Mascota{% endblock %}

{% block body %}

    <div style="background-color: #846355; min-height: 85vh; padding: 60px 0; display: flex; align-items: center;">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    
                    <div class="card border-0 shadow-lg" style="border-radius: 12px; padding: 2rem;">
                        <div class="card-body">
                            
                            <h2 class="mb-4 text-center" style="color: #000; font-weight: bold;">Editar Mascota</h2>

                            <form action="{{ path('app_admin_mascota_editar', {id: mascota.id}) }}" method="post" enctype="multipart/form-data">
                                
                                {# Nombre y Especie #}
                                <div class="row mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label text-muted small">Nombre</label>
                                        <input type="text" class="form-control" name="nombre" value="{{ mascota.nombre }}" required>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label text-muted small">Especie</label>
                                        <select class="form-select" name="especie">
                                            <option value="Perro" {{ mascota.especie == 'Perro' ? 'selected' : '' }}>Perro</option>
                                            <option value="Gato" {{ mascota.especie == 'Gato' ? 'selected' : '' }}>Gato</option>
                                        </select>
                                    </div>
                                </div>

                                {# Edad y Tamaño #}
                                <div class="row mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label text-muted small">Edad</label>
                                        <select class="form-select" name="edad">
                                            <option value="Cachorro" {{ mascota.edad == 'Cachorro' ? 'selected' : '' }}>Cachorro</option>
                                            <option value="Joven" {{ mascota.edad == 'Joven' ? 'selected' : '' }}>Jóven</option>
                                            <option value="Adulto" {{ mascota.edad == 'Adulto' ? 'selected' : '' }}>Adulto</option>
                                            <option value="Abuelo" {{ mascota.edad == 'Abuelo' ? 'selected' : '' }}>Abuelo</option>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label text-muted small">Tamaño</label>
                                        <select class="form-select" name="tamano">
                                            <option value="Pequeño" {{ mascota.tamano == 'Pequeño' ? 'selected' : '' }}>Pequeño</option>
                                            <option value="Mediano" {{ mascota.tamano == 'Mediano' ? 'selected' : '' }}>Mediano</option>
                                            <option value="Grande" {{ mascota.tamano == 'Grande' ? 'selected' : '' }}>Grande</option>
                                        </select>
                                    </div>
                                </div>

                                {# Imagen #}
                                <div class="mb-3">
                                    <label class="form-label text-muted small">Cambiar Foto (Dejar vacío para mantener actual)</label>
                                    <input type="file" class="form-control" name="imagen" accept="image/*">
                                    <div class="form-text">Foto actual: {{ mascota.imagen }}</div>
                                </div>

                                {# Descripción #}
                                <div class="mb-3">
                                    <label class="form-label text-muted small">Descripción</label>
                                    <textarea class="form-control" name="descripcion" rows="4" style="resize: none;">{{ mascota.descripcion }}</textarea>
                                </div>

                                {# Checkbox Disponible #}
                                <div class="form-check mb-4">
                                    <input class="form-check-input" type="checkbox" name="disponible" id="checkDisp" {{ mascota.disponible ? 'checked' : '' }}>
                                    <label class="form-check-label" for="checkDisp">
                                        Mascota Disponible para adopción
                                    </label>
                                </div>

                                {# Botones #}
                                <div class="d-flex justify-content-center gap-3">
                                    <a href="{{ path('app_admin_mascotas') }}" class="btn btn-outline-secondary px-4">Cancelar</a>
                                    <button type="submit" class="btn text-white px-5" style="background-color: #0F5D3C;">Guardar Cambios</button>
                                </div>

                            </form>
                            
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

{% endblock %}